# Server configuration
PORT=9999
ALLOW_ORIGIN=*
INSECURE_HTTP_ORIGINS=example.com,test.com

# Optional: Enable detailed logging
# Set NODE_ENV to 'development' for verbose logging
NODE_ENV=production 